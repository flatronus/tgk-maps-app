# Кресленик речень

SVG/HTML/JS-застосунок, що перетворює введені користувачем речення на графічні схеми: кожне слово — прямокутник з текстом, прямокутники з'єднані лініями. Кожне графічне речення виводиться в новому рядку, не перекриває сусідні та відступає від них.

Застосунок є PWA (Progressive Web App) — його можна встановити на телефон чи комп'ютер, і він працює офлайн після першого відкриття.

## Файли застосунку

- `index.html` — головний файл застосунку (весь інтерфейс і логіка).
- `manifest.json` — маніфест PWA (назва, іконки, кольори).
- `sw.js` — service worker для офлайн-роботи.
- `icon-192.png`, `icon-512.png` — іконки застосунку.

## Як розмістити на GitHub Pages

1. Створіть новий репозиторій на GitHub (наприклад, `sentence-diagram`).
2. Завантажте туди всі файли зі списку вище одним рівнем (без вкладених папок): `index.html`, `manifest.json`, `sw.js`, `icon-192.png`, `icon-512.png`, `README.md`.
3. У репозиторії відкрийте `Settings → Pages`.
4. У розділі «Build and deployment» оберіть `Source: Deploy from a branch`, гілку `main` (або `master`) і папку `/ (root)`, натисніть «Save».
5. Через 1–2 хвилини сайт стане доступним за адресою `https://<ваш-логін>.github.io/<назва-репозиторію>/`.
6. Відкрийте цю адресу в Chrome на телефоні чи комп'ютері — з'явиться можливість «Встановити застосунок» (Android/десктоп) або «Додати на головний екран» (iOS, через Safari → кнопка «Поділитись»).

Якщо після оновлення файлів у репозиторії зміни не з'являються на встановленому застосунку — це кешування service worker; підніміть номер версії в `sw.js` (змінна `CACHE_NAME`, наприклад `v1` → `v2`), закомітьте, і при наступному відкритті застосунок підвантажить свіжі файли.

## Журнал версій

- 2026-08-01 — v1.19.0 — 1) Зі шляху до проекту в шапці прибрано слово «Корінь» (і так зрозуміло, коли теки немає) та довге тире-роздільник замінено на пробіли. 2) У провіднику проектів додано перетягування тек і проектів між директоріями: у кожному рядку зліва — новий маркер-держак «⠿» (`pointerdown` на ньому починає перетягування; клік по назві/кнопках рядка на це не впливає). Під час перетягування ціль під пальцем/курсором визначається через `document.elementsFromPoint` (з резервом на `elementFromPoint`) і підсвічується рамкою; відпускання над текою переміщує елемент усередину неї, відпускання над кнопкою «← Назад» переміщує елемент на рівень вище (у батьківську теку поточної). Теку заборонено переносити в саму себе чи в її власну підтеку (перевірка через `collectProjectDescendantIds`, щоб не утворити цикл у дереві). Слідуючи перевіреному прийому з перетягування прямокутників/маркерів рамки: `setPointerCapture` на держаку, window-рівневі `pointermove`/`pointerup`/`pointercancel`, `preventDefault()` на нативному `touchmove` — список під час самого перетягування не перерендерюється (лише клас підсвітки), повний `renderProjectsList()` викликається один раз, після відпускання.
- 2026-08-01 — v1.18.2 — Виправлено розташування шляху до проекту в шапці: раніше він виводився окремим рядком під версією програми, тепер — праворуч від версії, в тому самому рядку (`#appPath` перенесено з окремого `<div>` під `<h1>` у `<span>` всередині самого `<h1>`, одразу після `.app-version`; додано роздільник «— » перед шляхом для читабельності в одному рядку).
- 2026-08-01 — v1.18.1 — Виправлено «урізання» графічного речення при синхронізації після розширення ланцюжка прямокутників праворуч. Причина: `sentence.root` зберігалось у хмару (Firestore) як вкладене дерево (`node.children` → знову вкладений `node.children` → ...), а кожен наступний прямокутник у ланцюжку праворуч додавав ще один рівень вкладеності JSON; Firestore відмовляється приймати документи, де карти/масиви вкладені глибше 20 рівнів, тож `setDoc()` після ~20 прямокутників у ланцюжку падав з помилкою (тихо ловилась у `.catch`), і в хмарі лишалась стара, коротша версія — звідси й «урізання» після синхронізації/перезавантаження. Виправлення: нові функції `flattenTree()`/`unflattenTree()` перетворюють вкладене дерево на пласку структуру `{ rootId, nodes: [...], edges: [...] }` (глибина JSON більше не залежить від довжини ланцюжка) — саме вона тепер зберігається і локально (IndexedDB), і в хмарі через `serializeSnapshot()`/`applySnapshot()`; уся інша логіка застосунку (рендер, перетягування) як і раніше працює з вкладеним деревом у пам'яті, перетворення відбувається лише на межі збереження/відновлення. Формат знімка підвищено (v1 → v2) зі зворотною сумісністю: раніше збережені вкладені дерева (без поля `nodes`) розпізнаються й застосовуються без змін. Тепер ланцюжок можна робити будь-якої довжини — він коректно зберігається і синхронізується. Версію кешу service worker підвищено (v37 → v38).
- 2026-08-01 — v1.18.0 — Додано масштабування «щипком» (pinch-to-zoom) двома пальцями на планшеті/смартфоні: слухачі `touchstart`/`touchmove`/`touchcancel` на `#contentScroll` рахують відстань між двома пальцями і викликають той самий `setZoom()`, що й кнопки +/−, з `preventDefault()` у `touchmove` (`{ passive: false }`), щоб браузер не запускав власний нативний зум сторінки поверх нашого. Також у шапці під версією програми додано рядок з повним шляхом до поточно відкритого проекту у форматі провідника файлів («Корінь / Тека / Підтека / Назва проекту») — новий елемент `#appPath`, оновлюється функцією `updateAppPathDisplay()` при відкритті/перемиканні проекту, перейменуванні (в тому числі батьківських тек) та при першому завантаженні застосунку. Версію кешу service worker підвищено (v36 → v37).
- 2026-07-31 — v1.17.0 — Додано підтримку кількох графічних книг («проектів»). Нова кнопка «🗂 Провідник проектів» — на верхній панелі, зліва від іконок типу лінії. У вікні провідника: створення тек (для групування) і проектів (кожен — окрема канва), перейменування «на місці» (✏) та видалення (🗑, з підтвердженням і каскадним видаленням вмісту теки), навігація по теках («← Назад» + заголовок поточної теки), відкриття проекту кліком по назві (поточний відкритий проект позначений бейджем «відкрито»). Архітектурно: STORAGE/TAGS/лічильники тепер завжди відображають вміст САМЕ ТОГО проекту, який зараз відкритий (`currentProjectId`); перемикання проекту (`openProject`) підміняє їх і перерендерює канву. Кожен проект зберігається в IndexedDB під власним ключем (`project:{id}`), дерево тек/проектів — під окремим ключем (`projectsTree`). У Firestore тепер дві сутності замість однієї: документ `meta/tree` (дерево тек/проектів, підписка завжди активна) і по документу `projects/{id}` на кожен проект (підписка активна лише на поточний відкритий проект, перепідключається при перемиканні). Кнопка «Зберегти» тепер відправляє в хмару і дерево, і вміст поточного проекту разом. API Firebase-модуля узагальнено до `watch(колекція, doc, колбек)`/`push(колекція, doc, дані)` замість прив'язки до одного фіксованого документа. Дані з попередньої (однопроєктної) версії автоматично мігруються в «Проект 1» при першому запуску цієї версії. Версію кешу service worker підвищено (v35 → v36).
- 2026-07-31 — v1.16.1 — Спрощено синхронізацію (v1.16.0): прибрано концепцію коду доступу й окреме вікно «Синхронізація» повністю — тепер один спільний документ у Firestore (`CLOUD_DOC_ID = 'main'`), без жодного налаштування. У хмару дані йдуть лише вручну: нова кнопка «Зберегти» (💾) на верхній панелі, зліва від кнопки «Теги». На нижній інформаційній панелі — простий текстовий індикатор: зелений напис «синхронізовано» після успішного натискання «Зберегти» або отримання новіших даних з іншого пристрою; червоний напис «не синхронізовано» після будь-якої зміни на канві (кожен `renderAll()` через `scheduleSave()` позначає стан як несинхронізований, окрім випадку, коли рендер викликаний щойно отриманими з хмари даними). Локальне автозбереження (IndexedDB) і фонове отримання оновлень з інших пристроїв (`onSnapshot` на спільному документі) лишились автоматичними й непомітними — вручну керується лише відправлення. Версію кешу service worker підвищено (v34 → v35).
- 2026-07-31 — v1.16.0 — Додано збереження стану: раніше застосунок нічого не зберігав — оновлення сторінки чи закриття вкладки стирало всю роботу. Тепер: 1) **Локальне автозбереження (IndexedDB)** — працює завжди, без інтернету: кожна значуща зміна (додавання/редагування/видалення/переставляння/теги) автоматично зберігається (`scheduleSave()` викликається наприкінці кожного `renderAll()`), а при відкритті сторінки стан відновлюється до першого рендеру. 2) **Синхронізація між пристроями (Firebase Firestore)** — опційна: нова кнопка «Синхронізація» в нижній панелі відкриває вікно, де вводиться персональний код доступу (той самий на планшеті й комп'ютері); після цього кожна зміна фоново (з дебаунсом 800мс) відправляється в хмару (`boards/{код}` у Firestore), а зміни з інших пристроїв прилітають миттєво через `onSnapshot` і застосовуються з правилом «новіше перемагає» (`updatedAt`, timestamp) — застарілі дані з хмари не затирають свіжіші локальні. Firebase підключено окремим `<script type="module">` (щоб не міняти тип основного класичного скрипта), що публікує `window.cloudSync` і подію `cloudsyncready`. Індикатор (кольорова крапка) показує стан: немає коду / підключення / синхронізується / синхронізовано / помилка. Версію кешу service worker підвищено (v33 → v34).
- 2026-07-31 — v1.15.2 — Виправлено перетягування прямокутників (переставляння/переприв'язку) на планшетах — на дотик воно не працювало. Причина: `.word-box` (сам прямокутник) не мав `touch-action: none`, а обробники `pointerdown`/`pointermove` не викликали `preventDefault()` на нативному `touchmove` — тож браузер на дотикових пристроях перехоплював жест як прокрутку тла (`.content-scroll`) ще до того, як спрацьовувала логіка перетягування, точнісінько та сама проблема, що вже була вирішена раніше для маркерів рамки. Застосовано той самий перевірений прийом: `touch-action: none` на `.word-box`, `setPointerCapture`/`releasePointerCapture` на вказівнику, пряме блокування прокрутки через `window.addEventListener('touchmove', blockScrollDuringNodeDrag, { passive: false })`, `document.elementsFromPoint` із запасним варіантом через `elementFromPoint`, і новий обробник `pointercancel` (`onNodeDragCancel`), що скасовує перетягування без застосування змін, якщо ОС перервала жест. Версію кешу service worker підвищено (v32 → v33).
- 2026-07-31 — v1.15.1 — Кнопки «▲»/«▼» для перестановки прямокутників (v1.15.0) прибрано — замінено на перетягування прямокутника мишею/пальцем прямо на канві, як у звичайних програмах для карт розуму. Існуючий механізм перетягування (`onNodeDragMove`/`onNodeDragEnd`, раніше — лише для переприв'язки в дочірні) тепер розпізнає три зони на прямокутнику-цілі: верхні ~30% висоти — «покласти братом ПЕРЕД ціллю», нижні ~30% — «покласти братом ПІСЛЯ цілі» (у обох випадках показується жовта лінія-індикатор місця вставки, новий елемент `#dragInsertIndicator`), середні ~40% — попередня поведінка «зробити дочірнім прямокутником цілі» (підсвітка рамки, як і раніше). Перестановка «братом» можлива лише в межах того самого графічного речення й лише якщо в цілі є батько (новий `reorderNodeAsSibling`); перетягнутий прямокутник приєднується до тієї самої групи впорядкування, що й сусід — до рамки, якщо сусід у рамці, або до звичайних дітей батька, якщо ні (з відповідним прибиранням зі списків `nodeIds` старих рамок). Заборона переставляти корінь речення чи класти вузол братом власного нащадка збережена. Версію кешу service worker підвищено (v31 → v32).
- 2026-07-31 — v1.15.0 — Дві зміни: 1) Текст усередині прямокутників (термін і уточнення) вирівняно по лівому краю замість центрування — оновлено CSS (`.word-text`, `.note-text`: `text-anchor: start`) і поле редагування (`.node-edit-input`: `text-align: left`), а координати `x` для тексту й `tspan` у `drawNodeBox` тепер розраховуються від лівого краю прямокутника (`node.x + BOX_PAD_X`) замість центру. 2) Додано можливість переставляти прямокутники одного рівня: дві нові кнопки в головному рядку вводу — «▲» (перемістити раніше/вище) і «▼» (перемістити пізніше/нижче), що з'являються поруч із кнопкою рамки. Переставляння міняє місцями вибраний прямокутник із сусіднім лише в межах тієї самої групи впорядкування — звичайні діти одного батька або діти, прикріплені до однієї рамки (`getSiblingOrderGroup`, `canMoveSelectedNode`, `moveSelectedNode`), — тобто порядок членів рамки й звичайних дітей батька перевпорядковуються незалежно одне від одного. Кнопки неактивні, якщо вибраний прямокутник — корінь речення або вже крайній у своїй групі. Версію кешу service worker підвищено (v30 → v31).
- 2026-07-31 — v1.14.2 — Виправлено накладання прямокутників по вертикалі. Причина: в `layoutTree`/`place()` для прямокутника, що має дітей, висота під нього самого (`node.height`) ніколи не враховувалась при резервуванні місця в потоці розкладки (`cursorY`) — резервувалась лише висота, яку фактично зайняли його прямі діти. Раніше це не проявлялось, бо висота прямокутника була майже завжди фіксованою (BOX_HEIGHT/HEADER_HEIGHT_WITH_NOTES), але тепер, коли довгий текст переноситься по словах у кілька рядків (v1.14.0), висота прямокутника-«предка» з коротким одним нащадком могла суттєво перевищити висоту, відведену під цього нащадка, — і прямокутник вилазив за межі свого піддерева, накладаючись на сусідній прямокутник поруч (або йдучи в мінус, за верхню межу). Тепер після центрування такого прямокутника рахується реальний виліт зверху й знизу відносно вже зайнятого дітьми простору: при вильоті знизу — курсор розкладки (`cursorY`) просто розширюється (наступний сусідній прямокутник розташовується нижче); при вильоті зверху — усе піддерево (сам прямокутник і вкладені) зсувається вниз (нова функція `shiftSubtreeY`) так, щоб верх прямокутника точно збігався з початком відведеного простору, а курсор так само розширюється. У результаті між будь-якими прямокутниками завжди зберігається щонайменше стандартний відступ (SIBLING_GAP), незалежно від того, наскільки виріс текст. Версію кешу service worker підвищено (v29 → v30).
- 2026-07-31 — v1.14.1 — Підпис тега («#тег») у лівому нижньому куті прямокутника тепер клікабельний: натискання на нього одразу відкриває вікно «Теги» зі списком усіх прямокутників, до яких застосовано саме цей тег (нова функція `openTagsModalForTag`, що йде напряму до `showTagNodesList`, минаючи головний список тегів). Кожен тег рендериться окремим елементом (а не одним рядком-конкатенацією) з невидимою збільшеною областю натискання навколо нього (`.node-tag-hit`) для впевненого тапу на дотикових екранах; клік по підпису тега більше не знімає/змінює вибір прямокутника (`e.stopPropagation()`). Версію кешу service worker підвищено (v28 → v29).
- 2026-07-31 — v1.14.0 — Три нові зміни: 1) З нижньої інформаційної панелі повністю прибрано індикатор «Додати до» (разом із розміткою, стилями та кодом оновлення тексту); статусна панель тепер показує лише кількість речень і масштаб. 2) Ширину прямокутників (терміну й уточнень) обмежено 250 пікселями — довший текст переноситься по словах (нова функція `wrapBoxText`, аналогічна до `wrapTitleText`, але для тексту самих прямокутників); якщо навіть окреме слово довше за максимальну ширину, воно розбивається по символах. Висота прямокутника (`headerHeight`/`height` у `layoutTree`) тепер рахується з урахуванням кількості рядків після перенесення. 3) Додано теги: нова кнопка «Теги» (🏷) зліва від «Редагувати текст» відкриває вікно керування тегами — створення, перейменування «на місці» та видалення тегів зі спільного списку (`TAGS`); клік по назві тега показує список усіх прямокутників (з усіх графічних речень), до яких його застосовано, з переходом і плавним прокручуванням канви до потрібного прямокутника (`navigateToNode`); щоб додати тег до прямокутника — вибрати його на канві, натиснути «Теги», у вікні натиснути «Вставити» (перемикає режим вставки) і клікнути потрібний тег зі списку — вікно закриється, а в прямокутнику з'явиться підпис у форматі «#тег» жовтим текстом у лівому нижньому куті (для цього кожен прямокутник резервує додаткові 16px висоти, якщо в нього є хоч один тег). Версію кешу service worker підвищено (v27 → v28).
- 2026-07-31 — v1.13.11 — На планшетах (iPad/Android) при появі екранної клавіатури тепер прокручується тільки тло/канва з графічними реченнями (`.content-scroll`), а не весь застосунок. Раніше клавіатура просто накривала нижню частину екрана поверх незмінного за розміром `body` (`position: fixed; inset: 0`), і браузер компенсував це власним панорамуванням усієї сторінки (включно з верхньою й нижньою панелями), щоб підняти поле вводу над клавіатурою. Додано нову функцію `initAdaptiveViewportHeight`, яка через `window.visualViewport` відстежує реальну видиму висоту екрана (без частини під клавіатурою) і записує її в CSS-змінну `--app-height`; висота `body` тепер залежить від цієї змінної. Коли клавіатура з'являється, `body` звужується до видимої висоти, і лише `.content-scroll` (flex-блок з канвою) стискається та лишається прокручуваним у межах цієї видимої області — верхня й нижня панелі фізично нерухомі. Версію кешу service worker підвищено (v26 → v27).
- 2026-07-31 — v1.13.10 — Виправлено хвилясту лінію та зсув внутрішньої структури дерева при створенні першого прямокутника, доданого «до рамки» (особливо помітно, коли рамка охоплює все графічне речення). Причина: технічно такий прямокутник додається як звичайний дочірній вузол у дереві (до «вузла-якоря» — останнього члена рамки), і базовий алгоритм розкладки (layoutTree → place) вважав його звичайною дитиною: центрував вузол-якір (а каскадно — і всіх його предків) з урахуванням цього нового вузла та «з'їдав» його висотою місце в загальному вертикальному потоці колонки, зсуваючи вниз усі інші, не пов'язані з рамкою вузли дерева. Хоча остаточну позицію такого прямокутника й так повністю перевизначає repositionFrameChildren, це відбувається вже ПІСЛЯ базової розкладки — тому сам факт його наявності встигав спотворити позиції реальних членів рамки, а отже й самі межі рамки, від яких рахується лінія (звідси й хвилястість замість прямої лінії). Тепер прямокутники з edge.frameGroupId виключено з розрахунку центрування батька та з витрати курсора основної колонки — вони розкладаються в ізольованому, одразу відкоченому назад проході курсора. Також виправлено видимий номер версії в шапці інтерфейсу (id="appVersion"), який після попередньої правки й далі показував стару цифру. Версію кешу service worker підвищено (v25 → v26).
- 2026-07-30 — v1.13.9 — Знайдено і виправлено справжню причину «стрибків» прямокутника на чужу рамку при розширенні межі рамки маркером (▲/▼, тап-режим): прямокутник, доданий «до рамки» ОДНІЄЇ групи (напр. зовнішньої), у дереві технічно є звичайним сиблінгом серед дітей свого «вузла-якоря» — і код розширення межі рамки (computeFrameDragTarget) це ніяк не враховував. Якщо такий «чужий доданий» прямокутник опинявся структурно поруч (у тому самому списку дітей), його можна було випадково захопити в ІНШУ (напр. внутрішню, вкладену) рамку — тоді він одночасно вважався б членом двох різних рамок, для яких дві незалежні частини коду рахують різні, суперечливі позиції, і він «стрибав» між ними. Тепер: (1) тап прямо на прямокутник, доданий «до» іншої рамки, ігнорується — він не захоплюється; (2) якщо розширення межі «перестрибує» через такий прямокутник до сиблінга за ним, діапазон обрізається одразу перед ним. Це стосується і живого попереднього перегляду під час тап-режиму, і остаточного застосування межі — обидва йдуть через ту саму функцію. Версію кешу service worker підвищено (v24 → v25).
- 2026-07-30 — v1.13.8 — Виправлено накладання сусіднього прямокутника на контур рамки: резерв вертикального місця перед рамкою під заголовок рахувався за грубою консервативною оцінкою «до 2 рядків», хоча перенесення тексту заголовка (wrapTitleText) рядків не обмежує — довгий заголовок міг зайняти 3 і більше рядків, і реальна висота рамки перевищувала зарезервоване місце, через що прямокутник над рамкою опинявся точно на її контурі чи заходив за нього. Тепер прогнозована ширина рамки (а отже й точна кількість рядків заголовка після перенесення) рахується заздалегідь — одразу після вимірювання розмірів прямокутників і побудови колонок по X, ще до розміщення по Y — тому зарезервоване місце завжди відповідає фактичній висоті заголовка, скільки б рядків він не займав. Версію кешу service worker підвищено (v23 → v24).
- 2026-07-30 — v1.13.7 — Виправлено помилку: якщо зовнішня та вкладена (внутрішня) рамки мали спільний прямокутник-член (наприклад, внутрішня рамка згрупована з підмножини членів зовнішньої), то при додаванні нового дочірнього прямокутника «до» зовнішньої рамки через цей спільний вузол новий прямокутник помилково потрапляв також у межі внутрішньої рамки, і внутрішня рамка розширювалась, охоплюючи чужий (зовнішній) доданий прямокутник. Причина: обхід піддерева для обчислення меж рамки пропускав лише гілки, додані «до тієї самої» рамки, але не враховував випадок, коли вузол зі спільним членством сам належить іншій, поточній рамці. Тепер такі гілки додатково пропускаються, якщо вузол, до якого приєднано «зовнішній» прямокутник, є прямим членом поточної (для якої рахуються межі) групи. Версію кешу service worker підвищено (v22 → v23).
- 2026-07-30 — v1.13.6 — Виправлено помилку «рамка в рамці»: якщо внутрішня (вкладена) рамка мала заголовок, зовнішня рамка могла не розширитись під неї, і внутрішня рамка з заголовком виступала за межі зовнішньої. Причина: чи «вкладена» одна рамка в іншу визначалось за прямокутниками, які вже враховували місце під заголовок — тому щойно доданий чи довший заголовок внутрішньої рамки міг зробити її прямокутник більшим за (ще нерозширену) зовнішню рамку, і код переставав розпізнавати вкладеність, а отже й не розширював зовнішню рамку. Тепер вкладеність визначається лише за межами самого вмісту рамок (без урахування заголовка), тому додавання чи зміна заголовка внутрішньої рамки більше не впливає на розпізнавання вкладеності — зовнішня рамка коректно розширюється й охоплює внутрішню разом з її заголовком. Версію кешу service worker підвищено (v21 → v22).
- 2026-07-30 — v1.13.5 — Знайдено підтверджену діагностикою причину `pointercancel`: логи з реального планшета показали, що `pointerdown` і `setPointerCapture` відпрацьовують коректно, `pointermove` викликається кілька разів (палець зсувається лише на ~8px по Y), після чого одразу приходить `pointercancel` — це класична поведінка браузера, який після невеликого порогу руху вирішує, що це жест прокрутки сторінки, і скасовує дотиковий жест. `touch-action: none`, встановлений на самому SVG-елементі маркера (`.frame-handle-hit`), виявився недостатнім для гарантованого блокування цього рішення браузера (відома нестабільність підтримки `touch-action` саме на SVG-елементах у частині мобільних браузерів). Додано пряме блокування нативної прокрутки: під час активного перетягування маркера рамки `window` тепер слухає нативну подію `touchmove` з `{ passive: false }` і викликає `preventDefault()` (функція `blockScrollDuringFrameDrag`) — це надійніший, незалежний від `touch-action` спосіб зупинити браузерну прокрутку. Слухач знімається при завершенні (`onFrameDragEnd`) чи скасуванні (`onFrameDragCancel`) перетягування. Версію кешу service worker підвищено (v20 → v21).
- 2026-07-30 — v1.13.4 — Діагностичне логування циклу перетягування маркера рамки: попередня версія логувала помилку лише через `console.error` у разі винятку в `onFrameDragMove`, тож якщо подія `pointermove` на реальному пристрої взагалі не доходила до `window` (типова причина — браузер обриває дотиковий жест подією `pointercancel`, яка раніше НІЯК не оброблялася), у консолі не з'являлося абсолютно нічого — ні помилки, ні підтвердження виклику. Додано явні `console.log('[frame-drag] ...')` на вхід у `pointerdown`, `pointermove` та `pointerup`, а також повністю новий обробник `pointercancel` (`onFrameDragCancel`) — раніше при скасуванні жесту браузером контур рамки міг «застрягати» червоним назавжди, оскільки стан `frameDrag` і візуальні класи ніколи не скидались. Тепер за будь-якого сценарію (успішний рух, помилка, скасування) у консолі буде видно, на якому саме етапі зупиняється жест — це дасть точну відповідь замість припущень. Версію кешу service worker підвищено (v19 → v20).
- 2026-07-30 — v1.13.3 — Захист `onFrameDragMove` від мовчазного обриву обробки жесту: якщо `document.elementsFromPoint` недоступний у браузері/WebView конкретного планшета (це не гарантовано у всіх мобільних оточеннях), виклик кидав виняток одразу на першому русі пальця — рамка встигала почервоніти при дотику (обробник `pointerdown` уже відпрацював), а далі жодного руху/підсвітки не відбувалося, бо виняток обривав усю подальшу логіку `onFrameDragMove` на кожному наступному русі. Додано резервний перехід на `document.elementFromPoint`, коли `elementsFromPoint` відсутній, і всю функцію обгорнуто в `try/catch` з виводом помилки в консоль (`console.error`), щоб будь-яка інша непередбачена помилка також не зупиняла обробку жесту мовчки. Перевірено емуляцією: розширення рамки й підсвітка сусіднього прямокутника коректно працюють навіть коли `document.elementsFromPoint` примусово прибрано. Версію кешу service worker підвищено (v18 → v19).
- 2026-07-30 — v1.13.2 — Знайдено й усунено справжню причину, чому сусідній прямокутник не підсвічувався під час перетягування пальцем маркера рамки: у `html, body` НЕ було заборони `user-select`/`-webkit-touch-callout`, тож на дотикових пристроях перетягування пальцем по екрану конкурувало з нативним браузерним жестом «виділення тексту по дотику» (саме тому виділявся текст на панелях) — цей нативний жест перехоплював дотик ще до/під час обробки в JS, тому власна підсвітка прямокутника й розширення рамки не встигали спрацювати, попри коректну логіку. Додано `user-select: none; -webkit-user-select: none; -webkit-touch-callout: none;` на `html, body` (тло/канва застосунку тепер повністю «непридатні» для виділення), і явно повернено виділення/редагування тексту в реальних полях вводу — головному полі речення (`#sentenceInput`) та полях редагування прямокутника (`.node-edit-input`), щоб введення й редагування тексту й надалі працювали як завжди. Версію кешу service worker підвищено (v17 → v18).
- 2026-07-30 — v1.13.1 — Виправлено підсвічування сусіднього прямокутника під час перетягування маркера рамки (▲/▼) на планшетах: `onFrameDragMove` визначав прямокутник під пальцем через `document.elementFromPoint()`, а поки маркер тримає pointer capture, деякі мобільні браузери (WebKit/Safari) через цю функцію повертають сам захоплений елемент маркера замість реального елемента в точці дотику — тому підсвічування й розширення рамки не спрацьовували взагалі. Замінено на `document.elementsFromPoint()` (повертає весь стек елементів у точці), з якого обирається перший елемент з атрибутом `data-node-id` — це надійно знаходить прямокутник під пальцем незалежно від того, що маркер потрапляє в стек. Версію кешу service worker підвищено (v16 → v17).
- 2026-07-30 — v1.13.0 — Перетягування маркера рамки (▲/▼) на планшетах повністю переведено на живий візуальний попередній перегляд: під час руху пальця більше НЕ викликається renderAll() (це знищувало DOM-елемент, захоплений пальцем, і зривало жест pointercancel'ом) — замість цього геометрія самої рамки оновлюється напряму через атрибути SVG, а фактична зміна межі групи застосовується лише при відпусканні маркера. Додано візуальний зворотний зв'язок: 1) контур рамки одразу фарбується в червоний (--danger) при дотику до маркера; 2) прямокутник, який зараз буде захоплено в рамку, підсвічується червоним контуром; 3) якщо маркер тягнуть за центр останнього прямокутника рамки (сигнал видалення групи) — контур рамки притлумлюється (напівпрозорий). Виправлено виділення тексту в прямокутниках: текст термінів і уточнень (`.word-text`, `.note-text`) більше не виділяється як для копіювання під час проведення пальцем по екрану — редагування, як і раніше, доступне лише через кнопку «Редагувати текст» («✏»). Версію кешу service worker підвищено (v15 → v16).
- 2026-07-28 — v1.12.0 — Компонування діаграми тепер поводиться за правилами, схожими на програми для ментальних карт: 1) прямокутник, доданий «до рамки», тепер вирівнюється за ОСТАТОЧНИМ (з урахуванням вкладеності в інші рамки) правим краєм рамки в два проходи розрахунку — якщо рамка розширюється вправо, цей прямокутник тепер іде за нею, а не лишається на місці всередині рамки. 2) У саму розкладку дерева (`layoutTree`) додано резервування додаткового вертикального відступу навколо кожного діапазону прямокутників-членів рамки (з урахуванням можливої вкладеності й висоти заголовка) — тому сусідні, не пов'язані з рамкою прямокутники тепер автоматично «відступають» (зсуваються вниз/вгору за звичайним компонуванням дерева), коли рамка займає більше місця, замість накладання на неї. Версію кешу service worker підвищено (v14 → v15).
- 2026-07-28 — v1.11.0 — Два виправлення: 1) заголовки-ярлички рамок групування та самі межі рамок більше не перекриваються, коли одна рамка структурно охоплює іншу (вкладену) — між межею зовнішньої рамки й вкладеною тепер завжди лишається відступ (22px); розмір і зсув діаграми коректно враховують цей відступ, щоб розширену рамку не обрізало. 2) Верхня й нижня панелі тепер надійно нерухомі під час прокрутки тла з графічними реченнями на дотикових пристроях (iPad/Android), навіть у встановленому як PWA застосунку — застосунок жорстко прив'язано до вікна перегляду (`position: fixed`) з `overscroll-behavior: none/contain`, щоб «гумовий» відскок сторінки не «зсовував» верхню панель з екрана; додано підтримку safe-area (`viewport-fit=cover`) для панелей у повноекранному PWA-режимі на пристроях із чубчиком/індикатором. Версію кешу service worker підвищено (v13 → v14).
- 2026-07-28 — v1.10.0 — Дві нові можливості з рамками групування: 1) якщо прямокутник додано «до» внутрішньої (вкладеної) рамки кнопкою «Додати», будь-яка ЗОВНІШНЯ рамка, що охоплює цю внутрішню, тепер розширюється й під цей прямокутник — раніше межі обчислення виключали такий прямокутник для БУДЬ-ЯКОЇ рамки, а не лише для тієї, до якої його додано; 2) рамку групування тепер можна застосувати і до кореневого прямокутника графічного речення (раніше кнопка «Рамка» була недоступна для кореня, бо в нього немає братів/сестер) — така рамка не має куди розширюватись, але створити її, дати їй заголовок і прибрати перетягуванням маркера за центр тепер можна. Версію кешу service worker підвищено (v12 → v13).
- 2026-07-28 — v1.9.4 — Три виправлення: 1) прямокутник, доданий кнопкою «Додати» при вибраній рамці, тепер ЗАВЖДИ примусово ставиться одразу за правим краєм рамки (з лінією від її краю, як і для будь-якого звичайного прямокутника) — раніше, якщо член групи вже мав власні дочірні прямокутники в тій самій колонці розкладки, новий «зовнішній» прямокутник міг візуально опинитися всередині рамки; ширина діаграми при потребі тепер також розширюється під це зміщення. 2) У обробник перетягування маркера рамки (▲/▼) додано `preventDefault()` — на планшетах без цього браузер під час перетягування міг перехопити жест як прокрутку сторінки, і розширення рамки дотиком не спрацьовувало. 3) Додано обробник кліку по порожньому тлу діаграми (не по прямокутнику, лінії, рамці чи маркеру) — раніше такого обробника не було зовсім, тому виділення й режим редагування тексту (кнопка «✏») не знімались після кліку повз усі елементи діаграми. Версію кешу service worker підвищено (v11 → v12).
- 2026-07-28 — v1.9.3 — Виправлено регрес із v1.9.0: прямокутник, доданий кнопкою «Додати» при вибраній рамці групування (створюється ЗОВНІ рамки, з'єднаний лінією від її краю), через розширення меж рамки на дочірні елементи (v1.9.0) помилково потрапляв усередину самої рамки. Тепер обчислення меж рамки не заходить у гілки, з'єднані як «до рамки» (frameGroupId) — такі прямокутники завжди лишаються зовні, а межі рамки, як і раніше, розширюються лише на справжніх нащадків прямокутників-членів групи. Версію кешу service worker підвищено (v10 → v11).
- 2026-07-28 — v1.9.2 — Виправлено помилку у злитті речень (v1.9.1): після приєднання кореневої гілки до іншого речення код звертався до неіснуючої змінної `activeFrame`, через що в консолі виникала помилка одразу ПІСЛЯ того, як дані вже коректно об'єднались, але ДО виклику перемальовування діаграми — тому злита гілка не з'являлась на екрані, доки користувач не клацав по якомусь прямокутнику (це й викликало власний виклик перемальовування). Хибне звернення прибрано, перемальовування тепер виконується одразу після злиття речень.
- 2026-07-28 — v1.9.1 — Виправлено два регреси з v1.9.0: 1) якщо згрупований у рамку прямокутник має дочірній елемент, маркери розширення рамки (▲/▼) тепер горизонтально розташовані точно над колонкою прямокутників-сиблінгів (а не по центру всієї, розширеної дочірнім прямокутником, рамки) — раніше через це вертикальне перетягування маркера більше не проходило над сусідніми сиблінгами і рамка не розширювалась на них; 2) перетягування кореневого прямокутника на прямокутник ІНШОГО графічного речення тепер приєднує ЦІЛЕ речення (кореневу гілку з усіма дочірніми прямокутниками й рамками) як дочірню гілку цільового прямокутника, тобто два речення стають одним деревом — раніше речення лише мінялись місцями в списку. Версію кешу service worker підвищено (v8 → v9).
- 2026-07-28 — v1.9.0 — Три виправлення роботи рамки групування та перетягування: 1) перетягування кореневого прямокутника графічного речення на прямокутник ІНШОГО графічного речення тепер переміщує ціле речення (усю кореневу гілку) на місце цільового речення в списку — раніше перетягування кореня повністю ігнорувалось, і кореневу гілку неможливо було перемістити; 2) рамка групування тепер розширюється не лише вгору/вниз, а й вправо — її межі рахуються з урахуванням усіх дочірніх прямокутників кожного згрупованого прямокутника (усієї гілки), тому новий дочірній прямокутник, доданий до вже згрупованого прямокутника, одразу опиняється в межах рамки, а рамки, що охоплюють різні гілки з дочірніми елементами, можуть візуально вкладатись одна в одну; 3) якщо в рамці лишився рівно один прямокутник, перетягування маркера рамки за його центр тепер прибирає рамку повністю — раніше звузити однобоксову рамку нижче одного елемента перетягуванням було неможливо, і останній прямокутник не міг «вийти» з рамки. Версію кешу service worker підвищено (v7 → v8).
- 2026-07-27 12:10 — v1.8.4 — Виправлено перетягування прямокутника на інший прямокутник (переприв'язку): 1) малювання ліній-з'єднань і прямокутників розділено на два окремі проходи (спершу всі лінії всього дерева, потім усі прямокутники) — раніше в деревах із кількома сиблінгами чи вкладеними дочірніми прямокутниками невидима «широка» лінія-хітбокс сусідньої гілки могла опинитися поверх прямокутника іншої гілки й перехоплювати перетягування, через що саме останній створений сиблінг або гілки з дочірніми прямокутниками інколи не вдавалося перетягнути; 2) перетягування прямокутника з одного графічного речення на прямокутник в іншому графічному реченні тепер працює коректно — кожен прямокутник додатково позначається номером свого речення, і цільове речення визначається саме за цим прямокутником, а не за реченням джерела перетягування (раніше пошук цілі завжди відбувався лише в межах речення, з якого починалось перетягування, тому міжреченнєве перетягування мовчки ігнорувалось). Версію кешу service worker підвищено (v6 → v7).
- 2026-07-27 11:20 — v1.8.3 — З нижньої панелі статусу прибрано кнопки переміщення прямокутника «▲»/«▼» разом з обслуговуючими їх функціями — переприв'язка прямокутника до іншого тепер виконується перетягуванням прямокутника мишею/дотиком на цільовий прямокутник (ця функція вже була реалізована раніше й продовжує працювати без змін). Версію кешу service worker підвищено (v5 → v6).
- 2026-07-27 01:05 — v1.8.2 — Довгий заголовок рамки групування тепер переноситься на кілька рядків (перенесення по словах, довгі слова розбиваються по символах), щоб текст завжди повністю вміщався в межах ширини рамки; висота рамки автоматично збільшується під потрібну кількість рядків. Виправлено розташування першого прямокутника, доданого «до рамки»: тепер він ставиться точно по центру горизонтальної осі рамки, тому лінія з'єднання виходить прямою (не дугоподібною); дугоподібними лінії стають лише тоді, коли до рамки додано вже кілька прямокутників — вони розташовуються стовпчиком навколо тієї ж осі. Біля заголовка застосунку «Кресленик речень» додано номер поточної версії. Для тегу `header` паддінг і маргін обнулено. Версію кешу service worker підвищено (v4 → v5).
- 2026-07-27 00:20 — v1.8.1 — Виправлено логіку кнопки «Додати» при вибраній рамці групування: тепер новий прямокутник створюється ЗОВНІ рамки, праворуч, і з'єднується лінією саме від краю рамки (як зазвичай прямокутник з'єднується з прямокутником), а не додається як ще один прямокутник усередину рамки. Виправлено баг, через який заголовок рамки міг бути невидимим (обрізався зверху діаграми, якщо рамка охоплювала найвищі прямокутники схеми) — тепер за потреби вся діаграма автоматично зсувається вниз, щоб заголовок завжди вміщався в межах видимої області. Версію кешу service worker підвищено (v3 → v4).
- 2026-07-27 00:00 — v1.8.0 — Виправлено розширення рамки групування на планшетах/телефонах: клікабельна зона маркерів ▲/▼ збільшена (невидиме коло радіусом 15px замість самого лише видимого маркера 6px) і отримала `touch-action: none` та захоплення вказівника (pointer capture), щоб дотик пальцем надійно розпочинав перетягування, а не прокрутку сторінки. Додано можливість додавати новий прямокутник прямо в рамку групування: якщо вибрано рамку, введений у полі текст і кнопка «+» («Додати») створюють новий прямокутник-визначення поруч з іншими в рамці й одразу включають його до групи. Додано заголовок рамки групування: при вибраній рамці кнопка «✏» вмикає режим редагування заголовка (поточний заголовок підставляється в поле вводу), введений текст зберігається як заголовок рамки натисканням «+»; заголовок відображається над рамкою. Версію кешу service worker підвищено (v2 → v3), щоб оновлені файли підвантажились одразу.
- 2026-07-26 20:44 — v1.7.1 — Якщо під час редагування повністю стерти текст уточнюючого рядка і зняти з нього фокус (клік поза полем або Enter), цей рядок тепер повністю видаляється з прямокутника (а не залишається порожнім) — прямокутник одразу зменшується під решту рядків.
- 2026-07-26 20:35 — v1.7.0 — Додано редагування тексту прямокутника: при вибраному прямокутнику зліва від кнопки видалення (🗑) з'явилась кнопка «✏» — вона вмикає режим редагування, і основний текст, і кожен уточнюючий рядок стають полями вводу прямо в прямокутнику; повторне натискання «✏» вимикає редагування. Режим редагування автоматично вимикається, щойно виділення прямокутника знімається або переходить на інший елемент. Прибрано вступні тексти «Схема речень · SVG-кресленик» та підказку під заголовком («Введіть текст і натисніть «+»…») — залишено лише заголовок «Кресленик речень».
- 2026-07-26 20:21 — v1.6.0 — Виправлено помилку: після зняття виділення з рамки групування її не вдавалося виділити повторно (клікабельною була лише тонка 1.5px пунктирна лінія рамки — практично неможливо влучити). Додано окрему широку невидиму «смугу» для клікабельної області рамки, тепер вибір/зняття вибору рамки клацанням працює надійно в обидва боки. Застосунок перетворено на PWA: додано `manifest.json`, `sw.js` (service worker для офлайн-роботи), іконки `icon-192.png`/`icon-512.png`, відповідні мета-теги в `index.html` (головний файл перейменовано з `sentence-diagram.html` на `index.html` для сумісності з GitHub Pages) та реєстрацію service worker. Додано інструкцію з розгортання на GitHub Pages в README.
- 2026-07-26 18:32 — v1.5.0 — Розширення рамки групування тепер відбувається перетягуванням маркера (кружечка) на верхньому чи нижньому краю вибраної рамки — на сусідні прямокутники визначень зверху/знизу, а не клацанням по них. Клацання на рамці вибирає її (з'являються маркери), повторне клацання знімає вибір. Кнопка видалення у верхній панелі тепер універсальна: працює для будь-якого вибраного елемента — вибраної рамки (видаляється лише групування, прямокутники всередині залишаються), вибраного прямокутника/лінії (видаляється прямокутник разом з усіма дочірніми прямокутниками) або цілого графічного речення, якщо вибрано кореневий прямокутник — з підтвердженням «Ок»/«Ні» для кожного випадку. Додано перетягування прямокутника термін/визначення на інший прямокутник мишею — при відпусканні над цільовим прямокутником створюється новий зв'язок/лінія (прямокутник разом із дочірніми переприв'язується до нової цілі); попередні кнопки переміщення «▲»/«▼» збережено й продовжують працювати.
- 2026-07-26 16:47 — v1.4.0 — Додано групування прямокутників визначень у рамку: кнопка «⬚» на верхній панелі накладає рамку на вибраний прямокутник, повторні клацання по сусідніх (зверху/знизу) прямокутниках визначень розширюють або звужують рамку, повторне натискання «⬚» завершує редагування рамки; рамку також можна повторно активувати клацанням по її межі. Додано переміщення вибраного прямокутника терміну/визначення вгору/вниз списку (кнопки «▲»/«▼» в нижній панелі статусу) — разом з прямокутником переміщуються і його дочірні прямокутники (для кореневого прямокутника переміщення міняє порядок самих графічних речень). Хрестик видалення справа від графічного речення прибрано; замість нього додано кнопку видалення (кошик) у верхній панелі, в одному рядку з іконками типів ліній, на протилежному (правому) боці. Натискання кнопки видалення графічного речення тепер відкриває модальне вікно підтвердження з кнопками «Ок» / «Ні».
- 2026-07-26 15:21 — v1.3.0 — Відстань між рядком терміну та рядком уточнення зменшено втричі (менша «шапка» прямокутника, коли є уточнення). Типовий тип лінії за замовчанням тепер суцільна (замість пунктирної); лінію зі стрілкою замінено на подвійну суцільну лінію; порядок іконок ліній: суцільна, пунктирна, подвійна. Рядок стану (лічильник речень, індикатор цілі, керування зумом) перенесено в самий низ сторінки. Верхня панель (заголовок, поле вводу, іконки ліній) і нижня панель стану тепер «намертво» закріплені відносно вікна й не зникають під час прокручування; прокрутка тепер спільна для всієї сторінки, а не для кожного окремого графічного речення. Кнопки «Додати» та «Уточнити» тепер лише з символами («+» і «✎») без напису. Додано керування зумом канви (кнопки «−»/«+», 50–200%). Додано адаптивну верстку для мобільних і планшетів (медіа-запити для менших екранів).
- 2026-07-26 13:06 — v1.2.0 — Мітка рядка спрощена до просто номера («01» замість «РЕЧ. 01»). Вибір прямокутника тепер перемикається повторним натисканням (клік вибирає, повторний клік на тому ж прямокутнику скасовує вибір); окрему кнопку «Скасувати вибір» прибрано. Селектор типу лінії замінено на ряд іконок-прикладів (пунктирна / суцільна / стрілка) без підписів, розміщений під полем вводу. Додано кнопку «Уточнити» — додає уточнюючий рядок тексту всередину вже вибраного прямокутника (без створення нового з'єднання), прямокутник збільшується під додаткові рядки. Додано можливість вибрати вже намальовану сполучну лінію кліком і замінити її тип через той самий ряд іконок.
- 2026-07-26 10:16 — v1.1.1 — Змінено напрямок побудови схеми: наступний прямокутник тепер малюється праворуч від попереднього (по колонках-рівнях), а не знизу. Сиблінги одного батьківського прямокутника розташовуються один під одним по вертикалі, без перекриття. З'єднувальні лінії тепер горизонтальні (з правого боку батьківського прямокутника до лівого боку дочірнього).
- 2026-07-26 10:13 — v1.1.0 — Змінено спосіб побудови схеми: тепер клацання на прямокутник вибирає його як ціль, поле вводу знає, куди додати новий прямокутник (індикатор «Додати до: …»), додано селектор типу сполучної лінії (пунктирна / суцільна / стрілка), прямокутники з'єднуються деревом (термін → визначення) з автоматичним non-overlapping layout. Прибрано кнопку «Очистити все». Видалення окремого графічного речення (цілого дерева) збережено та працює як раніше.
- 2026-07-26 10:02 — v1.0.0 — Перша версія. Реалізовано: поле вводу речення, побудова графічної схеми (прямокутники зі словами, з'єднані пунктирними лініями), список схем, де кожне нове речення додається новим рядком з відступом і не перекриває інші, кнопка видалення окремого речення, кнопка очищення всіх речень, лічильник доданих речень, дизайн у стилі креслярського "блюпринта".
